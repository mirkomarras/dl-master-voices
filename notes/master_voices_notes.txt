
- Pawel change combine_models (new version) script to show outputs and so on
- Existing vggvox.h5 does not work

EASY
+ Remove SRC folder and move one level top
+ Avoid use TF folders
+ Avoid model folder and rename like vggvox.py   
+ Move to __init__.py
x Cmd argument in separate files, main directory, main directory only contain the argument and import model and setup functions from other dirs

OTHER
- Integrate master voice train_wavegan script
+ tf.keras 2.0 moving vggvox (priority) + NOT USE placeholder
+ Track vggvox validation accuracy and check whether the training accuracy is behaving correctly (spectrogram normalization, layer implementation) 
+ Check batch size values, big batch size, v100 GPUs 

Thursday 19 11.30: 
+ Solve model saving pickles -> run vggvox train   
+ Solve resnet34/50 inference -> run resnet34/50 train 
+ Solve conv1D xvector -> run xvector train 
- Port wavegan -> run wavegan train 
- Integrate mv script -> run mv script
- Take a GAN for images, train on spectrograms and invert  
  
